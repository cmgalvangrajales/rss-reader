import { defineMessages } from 'react-intl';

const prefix = 'containers.Sider';

export default defineMessages({
  allArticles: {
    id: `${prefix}.allArticles`,
  },
  readLater: {
    id: `${prefix}.readLater`,
  },
  read: {
    id: `${prefix}.read`,
  },
});
